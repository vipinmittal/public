We are facing an issue in a React-based multi-valued date field component with drag-and-drop reordering.
🔍 Current Behavior
We have a date input field inside a multi-valued (repeatable) field component.
❌ Problem Scenario
User enters only the day (e.g., 26)
Month and Year are not selected yet.
User tries to reorder the items (drag & drop).
After reordering, the entered day value vanishes.
✅ Working Scenario
User enters a complete date (Day + Month + Year).
User reorders the items.
The value persists correctly.
🛠 Tech Stack
React
Multi-valued repeatable component
Drag-and-drop reordering
Controlled inputs (state-driven)
❓ What We Need Help With
We need help identifying:
Why the value disappears when only the day is entered.
Whether this is related to:
Controlled vs uncontrolled input behavior
State updates during reordering
Using array index as React key
Component remounting during drag
Date validation logic resetting incomplete values
Serialization/parsing issues when date is incomplete
🎯 Expected Behavior
Even if only the day is entered (without month and year), the value should remain preserved.
Reordering should not reset or clear partially entered values.
Component state should remain stable across drag operations.
