I want to replicate the exact CI failure locally.
I suspect the failure may be related to database configuration (CI might be using SQLite while my local setup uses MySQL), but I am not sure.
What I Need From You
Analyze the upstream .gitlab-ci.yml file and determine:
What database type is being used in CI
How Drupal is being installed in the pipeline
How Cypress tests are executed
Inspect my local DDev environment and confirm:
Current database driver
Whether SQLite is available in the PHP container
Determine whether I need:
To switch entirely to SQLite
Or create a separate SQLite test install
Or keep MySQL and adjust Cypress config only
Provide step-by-step commands to:
Replicate the CI environment locally
Install Drupal the same way CI does
Run the same Cypress tests locally
Match environment variables used in CI (if any)
Identify what changes (if any) are required in:
settings.php
settings.ddev.php
Cypress configuration
Environment variables
Important Constraints
Do NOT break my existing MySQL DDev setup.
If SQLite is needed, create a separate test configuration.
Ensure the local setup mimics the CI pipeline behavior as closely as possible.
Provide terminal commands I can run directly.
The objective is to reproduce the exact pipeline failure locally so I can debug and fix it.
